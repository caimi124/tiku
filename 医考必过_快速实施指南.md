# 🚀 "医考必过"快速实施指南

> 从"医药考试通"升级到"医考必过"的完整步骤

---

## 🎯 实施目标

将现有项目快速升级为"医考必过"品牌，实现：
- ✅ 视觉风格升级（新配色 + 新字体）
- ✅ 功能扩展（更多科目 + 押题卷）
- ✅ 营销优化（社群 + CTA）
- ✅ 支付集成（微信 + 支付宝）

---

## 📝 第一步：品牌视觉升级（2小时）

### 1.1 更新 Tailwind 配色

```bash
# 编辑 tailwind.config.ts
```

```typescript
// tailwind.config.ts
import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./components/**/*.{js,ts,jsx,tsx,mdx}",
    "./app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        // 主色调 - 医学蓝
        primary: {
          DEFAULT: '#007ACC',
          50: '#E6F4FF',
          100: '#CCE9FF',
          200: '#99D3FF',
          300: '#66BDFF',
          400: '#33A7FF',
          500: '#007ACC',
          600: '#0062A3',
          700: '#004A7A',
          800: '#003152',
          900: '#001929',
        },
        
        // 行动色 - 橙色
        action: {
          DEFAULT: '#FF6600',
          light: '#FF8533',
          dark: '#CC5200',
          hover: '#FF7719',
        },
        
        // VIP 金色
        vip: {
          DEFAULT: '#FFD700',
          light: '#FFF9E6',
          dark: '#B8860B',
        },
        
        // 成功/错误/警告（保持不变）
        success: { DEFAULT: '#10B981', light: '#D1FAE5' },
        error: { DEFAULT: '#EF4444', light: '#FEE2E2' },
        warning: { DEFAULT: '#F59E0B', light: '#FEF3C7' },
      },
    },
  },
  plugins: [],
};

export default config;
```

### 1.2 下载并配置思源黑体

```bash
# 1. 创建字体目录
mkdir -p public/fonts

# 2. 下载思源黑体
# 访问：https://github.com/adobe-fonts/source-han-sans/releases
# 下载 SourceHanSansCN-Regular.woff2, Medium.woff2, Bold.woff2

# 3. 将字体文件放到 public/fonts/ 目录
```

```typescript
// app/layout.tsx
import localFont from 'next/font/local';

const sourceHanSans = localFont({
  src: [
    { path: '../public/fonts/SourceHanSansCN-Regular.woff2', weight: '400' },
    { path: '../public/fonts/SourceHanSansCN-Medium.woff2', weight: '500' },
    { path: '../public/fonts/SourceHanSansCN-Bold.woff2', weight: '700' },
  ],
  variable: '--font-source-han-sans',
  display: 'swap',
});

export const metadata = {
  title: "医考必过 - 全科医学考试题库",
  description: "全科历年真题+押题卷，AI智能解析，助你一次通关",
  // ...
};

export default function RootLayout({ children }) {
  return (
    <html lang="zh-CN" className={sourceHanSans.variable}>
      <body className={sourceHanSans.className}>
        {children}
      </body>
    </html>
  );
}
```

### 1.3 替换首页

```bash
# 将 医考必过_优化版首页.tsx 的内容复制到 app/page.tsx
cp 医考必过_优化版首页.tsx app/page.tsx
```

---

## 🔧 第二步：数据库扩展（1小时）

### 2.1 更新 Prisma Schema

```bash
# 编辑 prisma/schema.prisma
```

```prisma
// 在 Question model 中添加新字段
model Question {
  // ... 原有字段
  
  // 新增字段
  examCategory   String  // 科目大类：药学/临床/口腔/检验/护理/中医
  questionSource String? // 题目来源：历年真题/模拟题/押题卷
  year           Int?    // 年份（真题）
  isPremium      Boolean @default(false) // 是否VIP专享
}

// 新增押题卷模型
model PredictionSet {
  id          String   @id @default(cuid())
  examType    String
  year        Int
  title       String
  description String?  @db.Text
  questionIds String[]
  isVip       Boolean  @default(true)
  price       Int?     // 单独购买价格（分）
  viewCount   Int      @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  @@index([examType])
  @@map("prediction_sets")
}

// 新增打卡记录
model CheckIn {
  id             String   @id @default(cuid())
  userId         String
  date           DateTime @default(now()) @db.Date
  questionsCount Int
  correctCount   Int
  streak         Int      // 连续打卡天数
  
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  @@unique([userId, date])
  @@index([userId])
  @@map("check_ins")
}

// 新增社群二维码
model CommunityQRCode {
  id          String   @id @default(cuid())
  type        String   // wechat/qq
  imageUrl    String
  description String?
  groupInfo   String?  // 群号或群名
  isActive    Boolean  @default(true)
  scanCount   Int      @default(0)
  createdAt   DateTime @default(now())
  
  @@map("community_qrcodes")
}
```

### 2.2 应用数据库变更

```bash
# 推送变更到数据库
npm run db:push

# 如果生产环境，使用迁移
npx prisma migrate dev --name add_yikaobiguo_features

# 重新生成 Prisma Client
npm run db:generate
```

### 2.3 更新种子数据

```bash
# 编辑 prisma/seed.ts，添加新科目的题目数据
```

---

## 💳 第三步：支付系统集成（3小时）

### 3.1 安装依赖

```bash
# 微信支付
npm install wechatpay-axios-plugin

# 支付宝
npm install alipay-sdk
```

### 3.2 配置环境变量

```bash
# .env.local
# 微信支付
WECHAT_PAY_APP_ID="your-app-id"
WECHAT_PAY_MCH_ID="your-merchant-id"
WECHAT_PAY_SERIAL="your-serial-no"
WECHAT_PAY_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
WECHAT_PAY_V3_KEY="your-v3-key"

# 支付宝
ALIPAY_APP_ID="your-app-id"
ALIPAY_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
ALIPAY_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"

# 网站URL
NEXT_PUBLIC_SITE_URL="https://yikaobiguo.com"
```

### 3.3 创建支付API

使用 Cursor AI 快速生成：

```
Prompt:
请帮我创建微信支付和支付宝的支付API，包括：
1. 创建支付订单
2. 支付回调处理
3. 订单查询
4. 退款功能

技术栈：Next.js 14 + TypeScript + Prisma
要求：完整的错误处理和日志记录
```

---

## 📱 第四步：社群功能（2小时）

### 4.1 创建二维码管理页面

```bash
# 在 Cursor 中使用 Prompt
```

```
Prompt:
请创建一个社群二维码管理页面（/admin/qrcodes），包括：
1. 上传二维码图片（微信群、QQ群）
2. 编辑二维码信息（群号、说明）
3. 启用/停用二维码
4. 查看扫描统计

使用 Next.js 14 + Prisma + CommunityQRCode 模型
```

### 4.2 创建打卡功能

```
Prompt:
请创建每日打卡功能，包括：
1. 打卡页面 (/checkin)
2. 显示连续打卡天数
3. 打卡日历视图
4. 打卡奖励（经验值）
5. 打卡排行榜

使用 CheckIn 模型
```

---

## 🎨 第五步：营销优化（2小时）

### 5.1 批量替换 CTA 按钮颜色

```bash
# 全局搜索并替换
# bg-primary-500 → bg-action
# hover:bg-primary-600 → hover:bg-action-hover
```

在 VS Code 中：
1. `Cmd/Ctrl + Shift + F` 全局搜索
2. 搜索：`bg-primary-500`
3. 替换为：`bg-action`
4. 搜索：`hover:bg-primary-600`
5. 替换为：`hover:bg-action-hover`

### 5.2 添加社群引流板块

在首页、做题页、会员页添加社群二维码展示：

```tsx
// components/CommunitySection.tsx
import { MessageCircle } from 'lucide-react';

export function CommunitySection() {
  return (
    <section className="bg-primary-500 text-white py-12 rounded-2xl">
      <div className="text-center">
        <MessageCircle className="w-12 h-12 mx-auto mb-4" />
        <h3 className="text-2xl font-bold mb-2">加入学习社群</h3>
        <p className="text-blue-100 mb-6">
          10,000+ 考生在这里分享经验、互相鼓励
        </p>
        <div className="flex justify-center gap-6">
          <div className="bg-white p-4 rounded-xl">
            <img src="/qr-wechat.jpg" className="w-40 h-40" alt="微信群" />
            <p className="text-gray-900 mt-2">微信学习群</p>
          </div>
          <div className="bg-white p-4 rounded-xl">
            <img src="/qr-qq.jpg" className="w-40 h-40" alt="QQ群" />
            <p className="text-gray-900 mt-2">QQ交流群</p>
          </div>
        </div>
      </div>
    </section>
  );
}
```

---

## 📊 第六步：数据分析（1小时）

### 6.1 集成百度统计

```html
<!-- app/layout.tsx -->
<script>
var _hmt = _hmt || [];
(function() {
  var hm = document.createElement("script");
  hm.src = "https://hm.baidu.com/hm.js?YOUR_BAIDU_CODE";
  var s = document.getElementsByTagName("script")[0]; 
  s.parentNode.insertBefore(hm, s);
})();
</script>
```

### 6.2 集成 Google Analytics 4

```bash
npm install @next/third-parties
```

```tsx
// app/layout.tsx
import { GoogleAnalytics } from '@next/third-parties/google'

export default function RootLayout({ children }) {
  return (
    <html>
      <body>{children}</body>
      <GoogleAnalytics gaId="G-XXXXXXXXXX" />
    </html>
  )
}
```

---

## 🚀 第七步：部署上线（2小时）

### 7.1 准备生产环境

```bash
# 1. 构建项目
npm run build

# 2. 测试生产版本
npm run start

# 3. 检查所有功能正常
```

### 7.2 部署到 Vercel

```bash
# 1. 推送代码到 GitHub
git add .
git commit -m "升级为医考必过品牌"
git push origin main

# 2. 在 Vercel 导入项目
# - 访问 vercel.com
# - Import Git Repository
# - 选择你的仓库
# - 配置环境变量（复制 .env.local 的内容）
# - Deploy

# 3. 配置自定义域名
# - Vercel Dashboard → Settings → Domains
# - 添加 yikaobiguo.com
# - 配置 DNS（在域名注册商处添加 CNAME 记录）
```

### 7.3 配置生产数据库

```bash
# 使用 Supabase 或其他云数据库
# 1. 创建生产数据库
# 2. 在 Vercel 添加生产数据库 URL
# 3. 运行迁移
npx prisma migrate deploy
```

---

## ✅ 上线检查清单

### 品牌视觉
- [ ] Logo 显示为"医考必过"
- [ ] 主色调为 #007ACC（蓝色）
- [ ] CTA 按钮为 #FF6600（橙色）
- [ ] 字体为思源黑体
- [ ] Favicon 已更新

### 功能完整性
- [ ] 6大科目分类正常显示
- [ ] 题目刷题功能正常
- [ ] AI 解析可用
- [ ] 错题本功能正常
- [ ] 会员权益展示清晰

### 支付系统
- [ ] 微信支付测试通过
- [ ] 支付宝测试通过
- [ ] 支付回调正常
- [ ] 会员自动开通

### 社群功能
- [ ] 二维码正常显示
- [ ] 打卡功能可用
- [ ] 社群板块布局合理

### 营销功能
- [ ] CTA 按钮位置合理
- [ ] Banner 展示正常
- [ ] 数据追踪配置完成

### SEO 优化
- [ ] Title: "医考必过 - 全科医学考试题库"
- [ ] Description 包含核心关键词
- [ ] Sitemap 提交到搜索引擎
- [ ] robots.txt 配置正确

---

## 📈 上线后运营计划

### 第1周：冷启动

**目标**：获取第一批种子用户

1. **内容准备**
   - [ ] 上传 1000+ 题目（各科目均衡）
   - [ ] 准备 5 套押题卷
   - [ ] 创建微信群、QQ群

2. **推广渠道**
   - [ ] 在医学论坛发帖（丁香园、医脉通）
   - [ ] 在知乎回答相关问题（带链接）
   - [ ] 在小红书发布学习笔记
   - [ ] 在微信公众号发文章

3. **转化优化**
   - [ ] 新用户注册送 7 天 VIP
   - [ ] 首月会员限时 ¥19
   - [ ] 分享拉新奖励

### 第2-4周：增长期

**目标**：DAU 500+，月收入 ¥10,000+

1. **内容运营**
   - [ ] 每天更新 50+ 题目
   - [ ] 每周发布 1 套押题卷
   - [ ] 每周更新 1 篇学习技巧文章

2. **社群运营**
   - [ ] 每天在群内分享题目
   - [ ] 组织每周打卡活动
   - [ ] 邀请考过的学员分享经验

3. **SEO 优化**
   - [ ] 发布 10+ 篇原创文章
   - [ ] 优化核心关键词排名
   - [ ] 建立 10+ 友情链接

### 第2-3月：规模化

**目标**：DAU 2000+，月收入 ¥50,000+

1. **付费推广**
   - [ ] 百度竞价（核心关键词）
   - [ ] 信息流广告（抖音、快手）
   - [ ] KOL 合作（医学博主）

2. **功能迭代**
   - [ ] 上线模拟考试功能
   - [ ] 开发移动端 App
   - [ ] 增加视频讲解

---

## 🎯 成功案例参考

### 类似产品分析

| 产品 | 月活 | 特色 | 可借鉴 |
|------|------|------|--------|
| 猿题库 | 500万+ | 智能推荐 | AI 功能 |
| 丁香园题库 | 50万+ | 专业权威 | 内容质量 |
| 医学教育网 | 100万+ | 直播课程 | 内容多样性 |

### 增长策略

1. **病毒式传播**：分享题目到朋友圈，好友点击可免费刷题
2. **社群裂变**：邀请 3 人免费升级 VIP
3. **内容营销**：在知乎、小红书持续输出干货
4. **口碑传播**：鼓励学员评价晒单

---

## 💡 常见问题

### Q1: 如何快速获取题目内容？

**A**: 
1. 使用 AI 生成（OpenAI GPT-4）
2. 购买题库版权（与出版社合作）
3. 用户贡献（UGC，审核后发布）
4. 从公开资源整理（注意版权）

### Q2: 微信支付需要什么资质？

**A**:
- 营业执照
- 对公银行账户
- 教育培训类目资质
- 在线教育内容审核

### Q3: 如何提高付费转化率？

**A**:
1. 免费试用（7天VIP）
2. 限时优惠（首月特价）
3. 社交证明（用户评价）
4. 紧迫感（仅剩 XX 个名额）
5. 无忧退款（7天内可退）

---

## 🎉 总结

按照以上步骤，你可以在 **2-3天** 内完成从"医药考试通"到"医考必过"的完整升级！

**时间分配**：
- Day 1: 品牌视觉 + 数据库扩展 + 首页替换（4小时）
- Day 2: 支付系统 + 社群功能 + 营销优化（6小时）
- Day 3: 测试 + 部署 + 上线（4小时）

**优先级建议**：
1. ✅ P0: 视觉升级 + 首页优化（立即完成）
2. ✅ P1: 支付系统 + 会员功能（1周内）
3. ✅ P2: 社群功能 + 打卡系统（2周内）
4. ✅ P3: 高级功能 + 移动端 App（1月内）

---

**现在就开始吧！祝你的"医考必过"项目大获成功！🚀🚀🚀**

需要帮助？随时查看：
- `医考必过_完整优化方案.md` - 详细方案
- `医考必过_优化版首页.tsx` - 首页代码
- `CURSOR_PROMPTS.md` - AI 开发助手

