# 🔧 获取正确的 Supabase 连接信息

## 📋 当前问题
- 直连模式: DNS 解析失败 (网络/防火墙问题)
- 连接池模式: 用户或租户未找到 (连接字符串格式问题)

## ✅ 解决步骤

### 1. 登录 Supabase Dashboard
访问: https://supabase.com/dashboard/project/tparjdkxxtnentsdazfw

### 2. 获取正确的数据库连接字符串

在项目页面，找到 **Settings** → **Database**，你会看到：

#### 🔸 Connection String (连接字符串)

有以下几种模式，请**全部复制**给我：

- **Transaction Mode** (事务模式 - 用于少量长连接)
  ```
  postgresql://postgres.[项目引用]:[密码]@aws-0-[区域].pooler.supabase.com:6543/postgres
  ```

- **Session Mode** (会话模式 - 推荐用于 Prisma)
  ```
  postgresql://postgres.[项目引用]:[密码]@aws-0-[区域].pooler.supabase.com:5432/postgres
  ```

- **Direct Connection** (直连模式)
  ```
  postgresql://postgres:[密码]@db.[项目引用].supabase.co:5432/postgres
  ```

### 3. 检查项目状态

在 Dashboard 中确认：
- ✅ 项目状态是 **Active** (激活)
- ✅ 数据库没有被暂停
- ✅ 项目引用 ID 是 `tparjdkxxtnentsdazfw`

### 4. 获取项目引用 (Project Reference)

在 Settings → General 中找到：
- **Reference ID**: 应该类似 `tparjdkxxtnentsdazfw`

## 📝 请提供以下信息

复制 Supabase Dashboard 中显示的完整连接字符串（包括所有模式），我会帮你测试哪个可用。

或者告诉我：
1. 项目状态是否正常？
2. 数据库是否需要"唤醒"（如果显示 Paused）？
3. 项目的 Region（区域）是什么？例如: ap-southeast-1

## 🔄 临时使用 API 模式

如果直连始终无法工作，我们可以：
1. 使用 Supabase JS Client（通过 HTTPS API）
2. 使用 PostgREST API 访问数据
3. 配置 Prisma Data Proxy（需要付费）

但首先让我们确认正确的连接字符串。
